\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,commandchars=\\\{\}}

% Allow better line breaking for regular text
\usepackage{microtype}
\sloppy

% Break long URLs
\usepackage{hyperref}
\usepackage{xurl}

% Adjust margins if needed (optional)
\usepackage[margin=1in]{geometry}

% pandoc report.md -s -o report.pdf -H wrap-code.tex
